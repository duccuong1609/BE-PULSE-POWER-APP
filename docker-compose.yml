version: '3.8'

services:
  pulse-power-app:
    image: duccuong1609/pulse-power-app:latest
    ports:
      - "8080:8080"
    env_file:
      - .env.production
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
